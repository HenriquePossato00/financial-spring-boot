spring:
  datasource:
    url: ${DB_URL}
    # username: postgres
    # password: Henrry00Us4t@21
    # driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    
  logging:
  level:
    org.postgresql: DEBUG